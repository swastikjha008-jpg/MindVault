# MindVault is frontend-only — there is no server and nothing to configure.
# All data (profile, notes, links, tasks, ideas, reading list) lives in the
# browser's Local Storage. This file is kept only as a placeholder in case
# you later swap the storage layer (apps/web/lib/storage) for a real backend.
